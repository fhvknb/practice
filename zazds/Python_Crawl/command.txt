docker run -ti -v /sys/fs/cgroup:/sys/fs/cgroup:ro -p 8000:8000 local/c7-systemd-httpd

export ALL_PROXY=socks5://127.0.0.1:7891

docker cp ./chromedriver_linux64.zip a7f03b21b326:root

export PATH="$PATH:/usr/local/bin/chromedriver"

# 添加定时任务 crontab -e
0 8,18 * * * command   # 任务格式